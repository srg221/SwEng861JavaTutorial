package prototyping;

import java.lang.reflect.*;
import java.util.*;

import java.util.Map;

public class ValidateMap {
	Map<Character, Method> validatorMap = new HashMap<Character, Method>();
	
	ValidateMap(Class className ){
        
		//for (Method validator : className.) {
        //	validatorMap.put(validator.class.getMethod("getTag").invoke(), validator.class.getMethod());
	}

	
}
